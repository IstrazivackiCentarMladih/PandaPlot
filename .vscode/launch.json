{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PandaPlot",
            "type": "debugpy",
            "request": "launch",
            "module": "pandaplot.app",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}